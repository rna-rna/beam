Modify the UploadDropzone Component
Locate the UploadDropzoneProps interface at the top of the file.
Add a new prop to the interface to control the visibility of the card.
tsx
Copy code
interface UploadDropzoneProps {
  onUpload: (files: File[]) => void;
  imageCount?: number;
  showGuestCard?: boolean;  // New prop for controlling the card visibility
}
Update the function signature to destructure this new prop and set a default value:
tsx
Copy code
export default function UploadDropzone({ onUpload, imageCount = 0, showGuestCard = false }: UploadDropzoneProps) {
Locate the section that renders the card at the bottom right of the page:
tsx
Copy code
<Card className="absolute bottom-6 right-6 w-96 shadow-lg">
Wrap this entire block in a conditional check for showGuestCard:
tsx
Copy code
{showGuestCard && (
  <Card className="absolute bottom-6 right-6 w-96 shadow-lg">
    <CardHeader>
      <CardTitle className="flex items-center gap-2">
        <Globe className="h-5 w-5" />
        Give it a go!
      </CardTitle>
    </CardHeader>
    <CardContent className="space-y-4">
      <p className="font-geist-mono text-sm">Guest Upload – Limited access.</p>
      <div className="space-y-4">
        <p className="text-sm text-muted-foreground">Create a free account to:</p>
        <ul className="space-y-2 text-sm text-muted-foreground">
          <li>• Enable comments and feedback</li>
          <li>• Bulk download files</li>
          <li>• Organize your projects</li>
        </ul>
        <button 
          onClick={() => setShowSignUpModal(true)} 
          className="text-sm font-medium text-primary hover:underline cursor-pointer dark:text-primary-foreground"
        >
          Unlock full features – Sign up for free
        </button>
      </div>
    </CardContent>
  </Card>
)}
Step 4: Update home.tsx to Show the Card
Locate and open src/pages/home.tsx or the file where UploadDropzone is rendered on the homepage.
Update the UploadDropzone component call to pass the showGuestCard prop:
tsx
Copy code
<UploadDropzone onUpload={handleUpload} showGuestCard={true} />
